import React from "react";

// Create a context object with a default value of an empty object {} and assign it to the UserContext variable  using the React.createContext() method.   

const UserContext = React.createContext();

export default UserContext;

//user context is provider and consumer 
 